<%- include('../partials/start.ejs') %>
<a href="/admin/productos/create" class="button is-link">Nuevo Producto</a>

<table class="table is-fullwidth mt-2">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripción</th>
      <th>Codigo</th>
      <th>Marca</th>
      <th>Categoria</th>
      <th>Precio</th>
      <th>Stock</th>
      <th>Opciones</th>
    </tr>
  </thead>
  <tbody>
    <% productos.forEach(function(producto) { %>

    <tr>
      <td><%=producto.nombre%></td>
      <td><%=producto.descripcion %></td>
      <td><%=producto.codigo %></td>
      <td><%=producto.marca %></td>
      <td><%=producto.categoria %></td>
      <td>$ <%=producto.precio.toFixed(2) %></td>
      <td><%=producto.stock.toFixed(2) %></td>
      <td>
        <div class="field has-addons">
          <p class="control">
            <a href="/admin/productos/<%=producto.id%>" class="button">
              <span class="icon is-small">
                <i class="fas fa-edit"></i>
              </span>
            </a>
          </p>
          <p class="control">
            <a href="/admin/productos/delete/<%=producto.id%>" class="button">
              <span class="icon is-small">
                <i class="fas fa-trash"></i>
              </span>
            </a>
          </p>
        </div>
      </td>
    </tr>
    <% }); %>
  </tbody>
</table>


<%- include('../partials/end.ejs') %>